# Setup

This folder holds "how we build" artifacts (workflows, tooling, and dev process references).
It is intentionally separate from `identity/` and `mesh/`, which hold product concept work.

## Workflows

- PR-centric Voice Manager workflow references (OpenHands + OpenRouter) live in `setup/workflows/`.
