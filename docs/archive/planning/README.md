# Planning Archive

This directory keeps historical planning material that is useful for project context but is not the canonical source of current product status.

Use these files as implementation history:

- `receipt-signing-hardening-plan.md`
- `demo-readiness-two-week-plan.md`
- `tickets.md`

For the public project overview, start with the repo root `README.md` and the active control-plane docs under `identity/control-plane/docs/`.

Historical-auth note:

- These archived planning files preserve then-current implementation assumptions.
- Local demo references to `x-umbra-tenant-id` remain historically accurate for auth-disabled dev flows.
- Current auth and ingress guidance in `identity/control-plane/docs/` supersedes any older header-trust planning language in this archive.
